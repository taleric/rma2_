<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- EditText Style -->
    <style name="PrimaryEditText">
        <item name="android:layout_marginBottom">12dp</item>
        <item name="android:padding">12dp</item>
        <item name="android:background">@drawable/edittext_background</item>
    </style>

    <!-- Text Styles -->
    <style name="PrimaryText">
        <item name="android:paddingStart">8dp</item>
        <item name="android:textColor">@color/black</item>
    </style>

    <style name="ButtonPrimary">
        <item name="android:background">@drawable/button_primary</item> <!-- Reference your shape drawable here -->
        <item name="android:textColor">@android:color/white</item> <!-- Text color for contrast -->
        <item name="android:padding">12dp</item> <!-- Adjust padding as needed -->
    </style>

    <style name="PlaceholderText">
        <item name="android:textColor">@color/placeholderColor</item>
    </style>

    <style name="PrimaryTextBold">
        <item name="android:textColor">@color/black</item>
        <item name="android:textStyle">bold</item>
    </style>
</resources>
